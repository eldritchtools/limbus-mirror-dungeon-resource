export const metadata = {
    title: "New Run Plan | Limbus Company Mirror Dungeon Resource",
    description: "Create a new run plan"
};

export default function NewRunPlanLayout({ children }) {
  return children;
}
