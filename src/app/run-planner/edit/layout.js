export const metadata = {
    title: "Edit Run Plan | Limbus Company Mirror Dungeon Resource",
    description: "Edit a run plan"
};

export default function EditRunPlanLayout({ children }) {
  return children;
}
