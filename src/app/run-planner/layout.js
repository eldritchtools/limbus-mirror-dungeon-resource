export const metadata = {
    title: "Run Planner | Limbus Company Mirror Dungeon Resource",
    description: "Run Planner"
};

export default function RunPlannerLayout({ children }) {
    return <>{children}</>
}