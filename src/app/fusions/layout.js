export const metadata = {
    title: "Fusions | Limbus Company Mirror Dungeon Resource",
    description: "Fusion gift recipes"
};

export default function FusionsLayout({ children }) {
    return <>{children}</>
}