export const metadata = {
    title: "Theme Packs | Limbus Company Mirror Dungeon Resource",
    description: "Theme Packs"
};

export default function ThemePacksLayout({ children }) {
    return <>{children}</>
}