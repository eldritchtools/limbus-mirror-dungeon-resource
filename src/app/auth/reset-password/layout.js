export const metadata = {
    title: "Reset Password | Limbus Company Mirror Dungeon Resource",
    description: "Reset password"
};

export default function ResetPasswordLayout({ children }) {
  return <>{children}</>;
}
