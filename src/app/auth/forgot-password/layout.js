export const metadata = {
    title: "Forgot Password | Limbus Company Mirror Dungeon Resource",
    description: "Forgot password"
};

export default function ForgotPasswordLayout({ children }) {
  return <>{children}</>;
}
