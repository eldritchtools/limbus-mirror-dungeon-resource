export const metadata = {
    title: "Floor Planner | Limbus Company Mirror Dungeon Resource",
    description: "Floor Planner"
};

export default function FloorPlannerLayout({ children }) {
    return <>{children}</>
}