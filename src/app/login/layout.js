export const metadata = {
    title: "Login | Limbus Company Mirror Dungeon Resource",
    description: "Login page"
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}